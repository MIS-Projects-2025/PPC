// Must match the actual row height / header height react-data-grid uses,
// since scroll math (which group is "at top", scrollToCell math, etc.)
// depends on it.
export const ROW_HEIGHT = 35;
export const HEADER_ROW_HEIGHT = 35;
